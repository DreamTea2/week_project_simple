<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zsd.codelab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_fruit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_fruit"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".list.CodeMainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".list.HorizonListActivity" />
        <activity android:name=".list.StaggerListActivity" />
        <activity android:name=".list.chat.SimpleChatActivity" />
    </application>

</manifest>