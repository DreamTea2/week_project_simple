<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <!--  网络权限   -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--  用户网络的状态   -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--  bugly 相关权限  -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STAT E" />

    <permission
        android:name="android.permission.READ_LOGS"
        android:protectionLevel="normal" />

    <application
        android:name=".app.InitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".SplashScreenActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".AlphaActivity" />
        <activity android:name=".CardScaleActivity" />
        <activity android:name=".AttributeActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MotionActivity" />
        <activity android:name=".MotionToolbarActivity" />
        <activity android:name=".MotionDrawableActivity" />
        <activity android:name=".FragmentRiggerActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".fragments.WeightTitleActivity" />
        <activity android:name=".fragments.CircleLoadingActivity" />

        <!--    QQ 互联登录    晚点再对接 -->
        <activity
            android:name="com.tencent.tauth.AuthActivity"
            android:launchMode="singleTask"
            android:noHistory="true">
            <!--            <intent-filter>-->
            <!--                <action android:name="android.intent.action.VIEW" />-->

            <!--                <category android:name="android.intent.category.DEFAULT" />-->
            <!--                <category android:name="android.intent.category.BROWSABLE" />-->
            <!--                <data android:scheme="tencent你的AppId" />-->

            <!--            </intent-filter>-->
        </activity>

        <activity
            android:name="com.tencent.connect.common.AssistActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="behind"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity android:name=".fragments.MyRecyclerActivity" />
        <activity android:name=".fragments.SelectActivity" />

        <!-- 配置APP ID -->
        <meta-data
            android:name="BUGLY_APPID"
            android:value="0a5aaaeb00" />
        <!-- 配置APP版本号 -->
        <meta-data
            android:name="BUGLY_APP_VERSION"
            android:value="1.0.1" /> <!-- 配置APP渠道号 -->
<!--      暂时没有获取渠道号  <meta-data-->
<!--            android:name="BUGLY_APP_CHANNEL"-->
<!--            android:value="<APP Channel>" />-->
        <!-- 配置Bugly调试模式（true或者false）-->
        <meta-data
            android:name="BUGLY_ENABLE_DEBUG"
            android:value="true" />

    </application>

</manifest>